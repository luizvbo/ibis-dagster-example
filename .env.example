# Which Ibis backend executes the pipeline: duckdb | polars | pyspark
IBIS_BACKEND=duckdb

# duckdb backend
IBIS_DUCKDB_PATH=warehouse.duckdb   # or ":memory:"

# pyspark backend (requires the pyspark extra + a JDK)
SPARK_MASTER=local[*]
SPARK_WAREHOUSE_DIR=./spark-warehouse
